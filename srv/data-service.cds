/*
using { db as my } from '../db/data';

service DataService {
    @readonly entity Purchase as SELECT from my.Purchase{*};

    function getData() returns String;
    function sum(x:Integer, y:Integer) returns Integer;
    function aggregate() returns Integer;
}
*/